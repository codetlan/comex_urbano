<%= form_for(@category, html: { role: "form", class: "form-horizontal" }) do |f| %>
    <% if @category.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

          <ul>
            <% @category.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <%= f.label :name, 'Nombre:', class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.text_field :name, class: 'form-control', required: 'required' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :link, 'Link:', class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.text_field :link, class: 'form-control', required: 'required' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :position, 'Posición:', class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.number_field :position, class: 'form-control', required: 'required' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :active, 'Activo:', class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.number_field :active, class: 'form-control', required: 'required' %>
      </div>
    </div>

    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <%= link_to 'Cancelar', categories_path, class: "btn btn-default" %>
        <button type="submit" class="btn btn-primary">Publicar Categoria</button>
      </div>
    </div>

<% end %>
