<table>
  <thead>
  <tr>
    <th>Title</th>
    <th>Introduction</th>
    <th>Description</th>
    <th>Active</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.introduction %></td>
        <td><%= post.description %></td>
        <td><a href="/posts/<%= post.id.to_s %>"><%= image_tag post.image.url(:square) %></a></td>
        <!--<td><= link_to 'Show', post %></td>
        <td><= link_to 'Edit', edit_post_path(post) %></td>
        <td><= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      </tr>
  <% end %>
  </tbody>
</table>

<br>