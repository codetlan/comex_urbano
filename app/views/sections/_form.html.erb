<%= form_for(@section, html: { role: "form", class: "form-horizontal" }) do |f| %>
    <% if @section.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@section.errors.count, "error") %> prohibited this section from being saved:</h2>

          <ul>
            <% @section.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <%= f.label :title, 'Titulo:', class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.text_field :title, class: 'form-control', required: 'required' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :description, 'Descripción:', class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.text_area :description, class: 'form-control ckeditor', required: 'required' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :main_banner, 'Banner Principal:', class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.file_field :main_banner, class: 'form-control', required: 'required' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :category_id, 'Categoria:', class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.select :category_id, Category.all.collect {|p| [p.name, p.id]}, class: 'form-control' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :active, 'Publicar:', class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <div class="radio">
          <label>
            <%= f.radio_button :active, '1' %>
            Si
          </label>
        </div>
        <div class="radio">
          <label>
            <%= f.radio_button :active, '0' %>
            No
          </label>
        </div>
      </div>
    </div>

    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <%= link_to 'Cancelar', sections_path, class: "btn btn-default" %>
        <button type="submit" class="btn btn-primary">Publicar Sección</button>
      </div>
    </div>
<% end %>
