<% site_banners.each do |banner| %>
    <% if banner.position == 0 %>
        <div class="banner-bottom">
          <div class="wrapper">
            <div class="cols-12">
              <div class="col col-12">
                <div class="col-inner">
                  <%= image_tag banner.photo.url(:bottom) %>
                </div>
              </div>
            </div>
          </div>
        </div>
    <% end %>
<% end %>
